STATUS: CANDIDATE, NOT APPROVED

# Price Path Level Interaction Class-Specific Checklist

This is a blocker checklist only.

## Conceptual Truth

- [ ] BLOCK if the watched level is missing or unnamed.
- [ ] BLOCK if the entrant population or trapped population lacks a named location.
- [ ] BLOCK if stop pool location is missing where the spec requires it.
- [ ] BLOCK if a latent pool is drawn as active before failure and reentry.
- [ ] BLOCK if user stop placement and crowd stop pool are not separated.
- [ ] BLOCK if the defended branch is absent.
- [ ] BLOCK if the professional alternative is missing where required.

## Encoding Integrity

- [ ] BLOCK if price direction or time direction is ambiguous.
- [ ] BLOCK if entry zones, pain zones, stop pools, and triggers use indistinct marks.
- [ ] BLOCK if logical invalidation and comfort stop are not visually distinct.
- [ ] BLOCK if forced exits look like fresh conviction.
- [ ] BLOCK if evidence is moved into rail text.

## Body Teaching

- [ ] BLOCK if the body does not teach location before outcome.
- [ ] BLOCK if the reader cannot tell whether the pool is latent or activated.
- [ ] BLOCK if the body reduces the lesson to P&L.
- [ ] BLOCK if the decision alternative is only prose.

## Density And Labels

- [ ] BLOCK if more than two callouts compete in one panel.
- [ ] BLOCK if price labels are not monospace.
- [ ] BLOCK if user stop and crowd stop labels overlap.
- [ ] BLOCK if rail text clips or becomes the teaching surface.
- [ ] STOP if `CH06_USER_STOP_INSIDE_CROWD_STOP_POOL_001` cannot fit without hiding the defended branch or professional alternatives.

## Renderer Boundary

- [ ] BLOCK if anyone claims renderer readiness for this figure type without adding an approved Layer-3 renderer later.
- [ ] BLOCK if a render, export, injection, or PNG gate is run during Layer-2.
- [ ] BLOCK if `qa.visual_snapshot` is changed.
