STATUS: CANDIDATE, NOT APPROVED

# Trap Storyboard Class-Specific Checklist

This is a blocker checklist. It does not approve a plate.

## Conceptual Truth

- [ ] BLOCK if the body does not show entry zone and forced-exit location before resolution.
- [ ] BLOCK if the displacement itself is treated as the trap trigger.
- [ ] BLOCK if the trap branch lacks reentry plus acceleration.
- [ ] BLOCK if acceleration is not tied to forced exits or liquidation.
- [ ] BLOCK if the defended branch is omitted, collapsed, or labeled as trap.
- [ ] BLOCK if no-trap-without-location, no-hindsight, and not-every-failed-breakout-is-a-trap are not visually protected.

## Encoding Integrity

- [ ] BLOCK if compared branches use different hidden price scales.
- [ ] BLOCK if the same initial displacement is not visually shared by the branches.
- [ ] BLOCK if stop pool, entry band, trigger, and target are indistinct.
- [ ] BLOCK if user or crowd stop concepts are merged when a target spec separates them.
- [ ] BLOCK if forced exits look like voluntary fresh demand or fresh selling.

## Body Teaching

- [ ] BLOCK if covering rails, caption, and footer removes the setup, fork, or branch read.
- [ ] BLOCK if required evidence ids are stamped or visually hosted only in rails.
- [ ] BLOCK if the body reads as a generic reversal chart.
- [ ] BLOCK if the fork point cannot be found before reading labels.

## Density And Labels

- [ ] BLOCK if labels overlap, badges stack, or leaders cross unnecessarily.
- [ ] BLOCK if more than two callouts compete in one panel.
- [ ] BLOCK if price labels are not monospace.
- [ ] BLOCK if evidence rail text clips or replaces body evidence.
- [ ] STOP if `CH06_FORCED_EXIT_CASCADE_001` cannot fit without shrinking type or removing the defended branch.

## Approval Boundary

- [ ] BLOCK if any file or asset claims approval.
- [ ] BLOCK if `qa.visual_snapshot` is changed.
- [ ] BLOCK if a renderer, export, injection, or PNG step is invoked as part of Layer-2 grammar work.
